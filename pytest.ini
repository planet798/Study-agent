[pytest]
testpaths = tests
# 无显示环境下跑 GUI 测试使用 offscreen 平台
qt_api = pyside6
markers =
    slow: 昂贵测试（迁移 / 集成 / 长模拟）；开发时可用 -m "not slow" 跳过
    migration: schema / legacy / release / verifier 迁移相关测试（必须走真实迁移路径）
    ui: 使用 qtbot 的 GUI / offscreen 测试
    integration: 端到端 / 多路线调度 / Phase 6 回归等集成测试
